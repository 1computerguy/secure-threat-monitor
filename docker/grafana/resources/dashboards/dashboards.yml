apiVersion: 1

providers:
  - name: "Dashboards"
    folder: "dashboards"
    type: file
    editable: true
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 300
    options:
      path: /etc/grafana/provisioning/dashboards